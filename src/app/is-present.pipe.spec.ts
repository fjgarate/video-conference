import { IsPresentPipe } from './is-present.pipe';

describe('IsPresentPipe', () => {
  it('create an instance', () => {
    const pipe = new IsPresentPipe();
    expect(pipe).toBeTruthy();
  });
});
